# Relative URL → requests go through Vite proxy → no CORS issues in development
# For production, change to your deployed backend: VITE_API_BASE_URL=https://api.yourapp.com/api
VITE_API_BASE_URL=/api
